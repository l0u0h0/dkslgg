"use client";

export default function Ranking() {
  return <div></div>;
}

// 탭 프로바이더를 만들어서 -> TabProvider.tsx
// props로 children, titles, setTabs를 받아서
// 탭 아이템 UI를 프로바이더에 구현하고 setTabs로 tabs 상태가 변경되면 새로운
