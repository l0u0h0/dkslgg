
// Line Graph Color
interface IlineColor {
  $gray: number;
  $blue: number;
}

// Horizen Graph Color 
interface IhorizenColor {
  $red: number;
  $gray: number;
}

// Label For File 
interface ILabelForFile {
  $for: string;
}